{{!-- <div class="container height-100 d-flex justify-content-center align-items-center">
    <div class="position-relative">
        <div class="card p-2 text-center">
            {{#if error}}
            <p style="color: red;">{{error}}</p>
            {{/if}}
            <h6>Please enter the one time password <br> to verify your account</h6>

            <form action="/validate-otp" method="post">
                <div id="otp" class="inputs d-flex  justify-content-center mt-2">
                    <input class=" form-control rounded" type="text" id="first" maxlength="6" name="otp"
                        style="width: 10rem;" />
                </div>
                <!-- userdata -->
                <input class="invisible" name="name" value="{{userdetails.Fname}}">
                <input class="invisible" name="email" value="{{userdetails.Lname}}">
                <input class="invisible" name="number" value="{{userdetails.Mobile}}">
                <input class="invisible" name="password" value="{{userdetails.Password}}">




                <!-- userdata -->

                <button type="submit" class="btn btn-danger px-4 validate mt-4">Validate</button>
            </form>
        </div>

    </div>
</div>
<link rel="stylesheet" href="/stylesheets/otppage.css"> --}}




<div class="d-flex justify-content-center align-items-center container">
    <div class="card py-5 px-3">

        <h5 class="m-0">Mobile Number verification</h5><span class="mobile-text">Enter the code we just send on your
            Mobile Phone <b class="text-danger">+91 {{userdetails.Mobile}}</b></span>
        {{#if error}}
        <p style="color: red;">{{error}}</p>
        {{/if}}
        <form action="/validate-otp" method="post">
            <div class="d-flex flex-row mt-3">
                <input type="text" name="otp" class="form-control"> 
            </div>
            <input class="invisible" name="number" value="{{userdetails.Mobile}}">
            <div class="text-center mt-4">
                <button type="submit" class="col-4 btn btn-outline-success text-center link-dark ">
                    Submit
                </button>
            </div>
        </form>
        {{!-- <div class="text-center mt-3"><span class="d-block mobile-text">Don't receive the code?</span><span
                class="font-weight-bold text-danger cursor">Resend</span></div> --}}
    </div>
</div>
<style>
    .card {
        width: 350px;
        padding: 10px;
        border-radius: 20px;
        background: #fff;
        border: none;
        height: 350px;
        position: relative;
    }

    .container {
        height: 100vh;
    }

    body {
        background: #eee;
    }

    .mobile-text {
        color: #989696b8;
        font-size: 15px;
    }

    .form-control {
        margin-right: 12px;
    }

    .form-control:focus {
        color: #495057;
        background-color: #fff;
        border-color: #ff8880;
        outline: 0;
        box-shadow: none;
    }

    .cursor {
        cursor: pointer;
    }
</style>